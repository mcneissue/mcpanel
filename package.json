{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "spago bundle-app --to bundle/bundle.js",
    "dev": "npm run build && npx parcel index.html"
  },
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.25.0"
  }
}
